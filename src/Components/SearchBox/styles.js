import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    borderWidth: 1,
    borderColor: "#c0c0c0",
    padding: 5,
    marginTop: 5,
  },

  list: {
    borderWidth: 1,
    borderTopWidth: 0,
    borderColor: "#c0c0c0",
    padding: 5,
    marginBottom: 20,
    maxHeight: 500,
  },
});
